{
  "name": "api-project",
  "version": "1.0.0",
  "description": "## Database Schema Design",
  "main": "index.js",
  "scripts": {
    "heroku-postbuild": "npm run build --prefix frontend && sequelize db:migrate && sequelize db:seed:all",
    "install": "npm --prefix backend install backend && npm --prefix frontend install frontend",
    "dev:backend": "npm install --prefix backend start",
    "dev:frontend": "npm install --prefix frontend start",
    "sequelize": "npm run --prefix backend sequelize",
    "sequelize-cli": "npm run --prefix backend sequelize-cli",
    "start": "npm start --prefix backend"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dsiwulec/API-project.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/dsiwulec/API-project/issues"
  },
  "homepage": "https://github.com/dsiwulec/API-project#readme",
  "dependencies": {
    "pg": "^8.8.0"
  }
}
